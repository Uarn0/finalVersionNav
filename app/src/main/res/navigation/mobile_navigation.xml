<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/navigation_list_transport">

    <fragment
        android:id="@+id/navigation_list_transport"
        android:name="com.example.cursova.ui.home.TransportHomeFragment"
        android:label="List of transport"
        tools:layout="@layout/fragment_home" >
        <action
            android:id="@+id/action_navigation_home_to_navigation_dashboard"
            app:destination="@id/navigation_add_transport" />
        <action
            android:id="@+id/action_navigation_home_to_navigation_notifications"
            app:destination="@id/navigation_list_event" />
    </fragment>

    <fragment
        android:id="@+id/navigation_add_transport"
        android:name="com.example.cursova.ui.addT.AddTransportFragment"
        android:label="Adding transport"
        tools:layout="@layout/fragment_add_transport" >
        <action
            android:id="@+id/action_navigation_dashboard_to_navigation_home"
            app:destination="@id/navigation_list_transport" />
    </fragment>

    <fragment
        android:id="@+id/navigation_list_event"
        android:name="com.example.cursova.ui.element.EventHomeFragment"
        android:label="List of event"
        tools:layout="@layout/fragment_second_home">

        <argument
            android:name="transportIndex"
            app:argType="integer" />
        <action
            android:id="@+id/action_navigation_list_event_to_navigation_add_event"
            app:destination="@id/navigation_add_event" />
    </fragment>

    <fragment
        android:id="@+id/navigation_add_event"
        android:name="com.example.cursova.ui.addE.AddEventFragment"
        android:label="Add event"
        tools:layout="@layout/fragment_add_event">
        <argument
            android:name="transportIndex"
            app:argType="integer" />
        <action
            android:id="@+id/action_navigation_add_event_to_navigation_list_event3"
            app:destination="@id/navigation_list_event" />
    </fragment>

</navigation>